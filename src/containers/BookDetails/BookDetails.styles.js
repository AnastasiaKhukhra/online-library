import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  image: {
    width: '400px',
    borderRadius: '15px',
    padding: '5px'
  },
  description: {
    width: '500px',
    borderRadius: '15px',
    backgroundColor: '#ffffff',
    marginBottom: '15px',
    padding: '0 10px'
  }
}));
